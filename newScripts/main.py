import random as r
import data
##prompt de geracao de imagem por IA

###categories.py###
from categories import *
##Chave API para uso da IA
API = "qnT3LUp3_FKiJJ8ohatYXQ" #Marjoram


#Ordenar da seguinte maneira
#teste = ["Naked", ebony, enhance, ",", pose, "at a", place, company, ",", extra, ",", style]
prompt = []
prompt.append("Naked")
for a in range(len(nega)):
    prompt.append(nega[a])
for b in range(len(enhance)):
    prompt.append(enhance[b])
prompt.append(",")
prompt.append(pose[0])
prompt.append(place[0])
prompt.append(place[1])
for c in range(len(companion)):
    prompt.append(companion[c])
for d in range(len(extra)):
    prompt.append(extra[d])
for e in range(len(style)):
    prompt.append(style[e])

#prompt final
print("Prompt de hoje:")
print(prompt)
print(' '.join(prompt))

###postagem
#Sharesome
#MOTHERLESS
#XVideos
# 
# 
##NOSTR
# Prioridade de orientação:
# Sharesome>MOTHERLESS>XVideos>Pornhub
# 18h > Logo, deve-se começar o processo 6h
# 18h; 18h10m; 18h20m; 18h30m; 18h40m
# 18h50: para caso algo aconteça
